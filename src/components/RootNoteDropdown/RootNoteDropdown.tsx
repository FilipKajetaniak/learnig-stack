import React, { FC } from "react";

import { Button } from "components/Button/Button";

import style from "./RootNoteDropdown.module.scss";

export const RootNoteDropdown: FC = () => {
  return <Button>C#</Button>;
};
